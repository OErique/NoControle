-- Notifications table is now created in 001-create-schema.sql
-- This file is kept for compatibility but does nothing
-- All notifications logic is already in the main schema

SELECT 'Notifications table already exists in main schema' AS message;
